#include<iostream>
#include<string>
using namespace std;
int main(){
    int array[]={2,5,6,8,9,4};
    for(int  i=0; i<=5; i++){ //here how the for loop work first it check the initial value the check the condition and execute the block of code and than increment the value of i and again check the condition in this ways the for loop work
        // cout<<"The value is :"<<array[i]<<endl;//if we directle access the array instead of i value we get the memory address of all those value we have to access the i value of those bcz i is iterate over this element
        }
    string names[]={"sameer ali","umair","faris","faria hussain"};
    for(int i=0; i<4; i++){
        cout<<"the value are :"<<names[i]<<endl;
    }    
}
